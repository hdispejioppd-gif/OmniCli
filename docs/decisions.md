# Архитектурные решения

## 2026-07-13: Создан Obsidian-ваулт

- Решено: вести базу знаний проекта в папке `docs/` как Obsidian-ваулт.
- Причина: сохранять контекст между сессиями, структурировать знания, помогать AI понимать проект.

## Принятые подходы проекта

- **Provider-neutral архитектура**: модуль `provider` абстрагирует любую LLM.
- **Explicit permissions**: любая опасная операция требует флага.
- **SHA-256 + exact-match для патчей**: предотвращает случайные конфликты при правках.
- **SQLite journaling**: все запуски и события сохраняются локально.
- **Versioned NDJSON events**: протокол событий версионируется для обратной совместимости.
- **Workflows как YAML DAG**: нет шаблонов, переменных окружения, скрытого shell.

## 2026-07-13: Исправлена сборка фрагментированных tool calls в OpenAI-стриминге

- Решено: эмитировать `ToolCall` при получении `finish_reason: "tool_calls"`, а не только на `[DONE]`.
- Причина: OpenAI может отправить usage-чанк между финальным tool-call и `[DONE]`; эмиссия по `finish_reason` сохраняет корректный порядок событий и позволяет парсить аргументы сразу после завершения.

## 2026-07-13: LM Studio реализован поверх OpenAI-compatible адаптера

- Решено: не создавать отдельный провайдер, а использовать существующий `OpenAiProvider` с пустым API-ключом.
- Причина: LM Studio полностью совместим с `/chat/completions`; отдельный код не нужен. Пустой ключ означает отсутствие `Authorization`-заголовка.
- Последствия: `OpenAiProvider::new` теперь пропускает заголовок `Authorization`, если ключ пуст.

## 2026-07-13: llama.cpp — отдельный нативный адаптер

- Решено: реализовать llama.cpp как отдельный `LlamaCppProvider`, использующий нативный endpoint `/completion`.
- Причина: llama.cpp server имеет собственный формат запроса/ответа, отличный от OpenAI chat completions. Отдельный адаптер демонстрирует гибкость provider-абстракции.
- Ограничения: нативный `/completion` не поддерживает tool calls. Для tool calls с llama.cpp рекомендуется `--provider lm-studio` / `--provider openai-compatible`.
- Промпт: используем ChatML-формат (`<|im_start|>role\n...\n<|im_end|>`) как разумный default; модель хранится в конфиге, но не передаётся в запрос.

## Конвенции

- Rust edition 2024.
- Конфигурация через `omni.toml` или CLI-флаги (флаги приоритетнее).
- API-ключи только через env-переменные, никогда не хранятся в конфиге.
